# Witty 浏览器接管桥

此扩展只在工作台选择“接管”并启动任务时连接本机回环桥，使用 Chrome
`debugger` 权限借用当前活动标签页。任务结束、服务断开或切回专用模式后会解除调试，
不会关闭被借用的标签页。

开发环境安装：打开 `chrome://extensions`，启用“开发者模式”，选择“加载已解压的
扩展程序”，目录指向本目录。

扩展只连接 `127.0.0.1:18770-18779`，网页来源不能接入后端 CDP 通道。
